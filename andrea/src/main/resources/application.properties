spring.application.name=andrea
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password= # ? REMOVIDO

spring.jpa.hibernate.ddl-auto=update
server.servlet.session.timeout=30m

# Níveis de log
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Configurações da API (sensíveis removidas)
phantombuster.api.base-url=https://api.phantombuster.com/api/v2
phantombuster.api.key= # ? REMOVIDO

# Valores padrão do phantom (dados pessoais removidos)
default.phantomId=
default.searchUrl=
default.sessionCookie=

